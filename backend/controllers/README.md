# Controllers

This directory will contain controller files that handle the business logic for different routes.

## Example Structure (Future):
- `transactionController.js` - Handle transaction CRUD operations
- `userController.js` - Handle user operations
- `categoryController.js` - Handle category operations

Controllers receive requests from routes, interact with models, and send responses back to the client.
